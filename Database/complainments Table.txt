CREATE TABLE complainments(
  orderId varchar2(255) NOT NULL,
  customer_email varchar2(255) NOT NULL,
  title varchar2(255) NOT NULL,
  description varchar2(255) NOT NULL,
  status Number,
  FOREIGN KEY (customer_email) REFERENCES customers(c_email)
);